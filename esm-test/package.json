{
  "name": "esm-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "pretest": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "catalog:dev",
    "zod": "catalog:dev",
    "zod-sockets": "workspace:*"
  }
}
