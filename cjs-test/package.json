{
  "name": "cjs-test",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "pretest": "tsc --noEmit",
    "test": "vitest run",
    "posttest": "rm *.js"
  },
  "devDependencies": {
    "typescript": "catalog:dev",
    "zod": "catalog:dev",
    "zod-sockets": "workspace:*"
  }
}
